# Project-wide Gradle settings (portable only — machine-specific paths belong in
# the user-level ~/.gradle/gradle.properties, e.g. org.gradle.java.home).
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
